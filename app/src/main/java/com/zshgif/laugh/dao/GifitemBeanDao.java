package com.zshgif.laugh.dao;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import com.zshgif.laugh.dao.db.DaoSession;
import com.zshgif.laugh.model.GifitemBean;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table GIFITEM_BEAN.
*/
public class GifitemBeanDao extends AbstractDao<GifitemBean, Long> {

    public static final String TABLENAME = "GIFITEM_BEAN";

    /**
     * Properties of entity GifitemBean.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property NETid = new Property(1, long.class, "NETid", false, "NETID");
        public final static Property Content = new Property(2, String.class, "content", false, "CONTENT");
        public final static Property Category_name = new Property(3, String.class, "category_name", false, "CATEGORY_NAME");
        public final static Property FirstOne = new Property(4, String.class, "firstOne", false, "FIRST_ONE");
        public final static Property GifUrl = new Property(5, String.class, "gifUrl", false, "GIF_URL");
        public final static Property Width = new Property(6, int.class, "width", false, "WIDTH");
        public final static Property Height = new Property(7, int.class, "height", false, "HEIGHT");
        public final static Property Type = new Property(8, int.class, "type", false, "TYPE");
        public final static Property Digg_count = new Property(9, int.class, "digg_count", false, "DIGG_COUNT");
        public final static Property Bury_count = new Property(10, int.class, "bury_count", false, "BURY_COUNT");
        public final static Property Comments_count = new Property(11, int.class, "comments_count", false, "COMMENTS_COUNT");
        public final static Property Share_url = new Property(12, String.class, "share_url", false, "SHARE_URL");
    };

    private DaoSession daoSession;


    public GifitemBeanDao(DaoConfig config) {
        super(config);
    }
    
    public GifitemBeanDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'GIFITEM_BEAN' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'NETID' INTEGER NOT NULL ," + // 1: NETid
                "'CONTENT' TEXT NOT NULL ," + // 2: content
                "'CATEGORY_NAME' TEXT NOT NULL ," + // 3: category_name
                "'FIRST_ONE' TEXT NOT NULL ," + // 4: firstOne
                "'GIF_URL' TEXT," + // 5: gifUrl
                "'WIDTH' INTEGER NOT NULL ," + // 6: width
                "'HEIGHT' INTEGER NOT NULL ," + // 7: height
                "'TYPE' INTEGER NOT NULL ," + // 8: type
                "'DIGG_COUNT' INTEGER NOT NULL ," + // 9: digg_count
                "'BURY_COUNT' INTEGER NOT NULL ," + // 10: bury_count
                "'COMMENTS_COUNT' INTEGER NOT NULL ," + // 11: comments_count
                "'SHARE_URL' TEXT NOT NULL );"); // 12: share_url
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'GIFITEM_BEAN'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, GifitemBean entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getNETid());
        stmt.bindString(3, entity.getContent());
        stmt.bindString(4, entity.getCategory_name());
        stmt.bindString(5, entity.getFirstOne());
 
        String gifUrl = entity.getGifUrl();
        if (gifUrl != null) {
            stmt.bindString(6, gifUrl);
        }
        stmt.bindLong(7, entity.getWidth());
        stmt.bindLong(8, entity.getHeight());
        stmt.bindLong(9, entity.getType());
        stmt.bindLong(10, entity.getDigg_count());
        stmt.bindLong(11, entity.getBury_count());
        stmt.bindLong(12, entity.getComments_count());
        stmt.bindString(13, entity.getShare_url());
    }

    @Override
    protected void attachEntity(GifitemBean entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public GifitemBean readEntity(Cursor cursor, int offset) {
        GifitemBean entity = new GifitemBean( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getLong(offset + 1), // NETid
            cursor.getString(offset + 2), // content
            cursor.getString(offset + 3), // category_name
            cursor.getString(offset + 4), // firstOne
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // gifUrl
            cursor.getInt(offset + 6), // width
            cursor.getInt(offset + 7), // height
            cursor.getInt(offset + 8), // type
            cursor.getInt(offset + 9), // digg_count
            cursor.getInt(offset + 10), // bury_count
            cursor.getInt(offset + 11), // comments_count
            cursor.getString(offset + 12) // share_url
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, GifitemBean entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setNETid(cursor.getLong(offset + 1));
        entity.setContent(cursor.getString(offset + 2));
        entity.setCategory_name(cursor.getString(offset + 3));
        entity.setFirstOne(cursor.getString(offset + 4));
        entity.setGifUrl(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setWidth(cursor.getInt(offset + 6));
        entity.setHeight(cursor.getInt(offset + 7));
        entity.setType(cursor.getInt(offset + 8));
        entity.setDigg_count(cursor.getInt(offset + 9));
        entity.setBury_count(cursor.getInt(offset + 10));
        entity.setComments_count(cursor.getInt(offset + 11));
        entity.setShare_url(cursor.getString(offset + 12));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(GifitemBean entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(GifitemBean entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
